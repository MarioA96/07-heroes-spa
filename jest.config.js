export default {
    testEnvironment: 'jest-environment-jsdom',
    setupFiles: ['./jest.setup.js'],
    // transformIgnorePatterns: [
    //     "node_modules/(?!(decode-uri-component|query-string)/)"
    // ]
    // transformIgnorePatterns: [
    //     "node_modules/(?!(query-string|decode-uri-component)).+\\.js$"
    // ]
}